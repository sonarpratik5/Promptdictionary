# Supabase project settings (Project Settings → API)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Used for email confirmation and password-reset links in non-local environments.
# For Netlify production, set this to https://neon-hamster-764629.netlify.app.
# Add `${NEXT_PUBLIC_SITE_URL}/auth/callback` to Supabase Auth's allowed Redirect URLs.
NEXT_PUBLIC_SITE_URL=http://localhost:3000
